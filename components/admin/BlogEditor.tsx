import { saveGitHubBlogPost } from "@/lib/github/content-actions";
type Initial = { slug?: string; status?: string; date?: string; title?: string; excerpt?: string; body?: string; sourceUrl?: string; organization?: string; location?: string };
export default function BlogEditor({ initial = {} }: { initial?: Initial }) { return <form action={saveGitHubBlogPost} className="glass-card mt-6 grid gap-4 rounded-2xl p-5"><div className="grid gap-4 sm:grid-cols-2"><label>Slug<input name="slug" required defaultValue={initial.slug} className="mt-1 w-full rounded-xl border border-border bg-card px-3 py-2" /></label><label>Status<select name="status" defaultValue={initial.status || "draft"} className="mt-1 w-full rounded-xl border border-border bg-card px-3 py-2"><option value="draft">Draft</option><option value="published">Published</option><option value="archived">Archived</option></select></label></div><input type="hidden" name="locale" value="en" /><label>Date<input name="date" type="date" required defaultValue={initial.date} className="mt-1 w-full rounded-xl border border-border bg-card px-3 py-2" /></label><label>Title<input name="title" required defaultValue={initial.title} className="mt-1 w-full rounded-xl border border-border bg-card px-3 py-2" /></label><label>Excerpt<textarea name="excerpt" defaultValue={initial.excerpt} className="mt-1 min-h-20 w-full rounded-xl border border-border bg-card px-3 py-2" /></label><label>Markdown content<textarea name="body" defaultValue={initial.body} className="mt-1 min-h-64 w-full rounded-xl border border-border bg-card px-3 py-2" /></label><div className="grid gap-4 sm:grid-cols-3"><label>Source URL<input name="sourceUrl" defaultValue={initial.sourceUrl} className="mt-1 w-full rounded-xl border border-border bg-card px-3 py-2" /></label><label>Organization<input name="organization" defaultValue={initial.organization} className="mt-1 w-full rounded-xl border border-border bg-card px-3 py-2" /></label><label>Location<input name="location" defaultValue={initial.location} className="mt-1 w-full rounded-xl border border-border bg-card px-3 py-2" /></label></div><button className="w-fit rounded-full bg-foreground px-5 py-2.5 text-sm font-medium text-background">Save and commit</button></form>; }

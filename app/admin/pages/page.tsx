import Link from "next/link";
import AdminShell from "@/components/admin/AdminShell";
import { requireAdmin } from "@/lib/admin/auth";
export default async function PagesPage() { await requireAdmin(); return <AdminShell><h1 className="text-4xl font-semibold tracking-[-.05em]">Pages / Sections</h1><p className="mt-3 max-w-2xl text-muted">Structured content only — visual layout and animations remain safely controlled by the portfolio code.</p><div className="mt-6 grid gap-3"><Link href="/admin/pages/home" className="glass-card rounded-2xl p-4 transition-colors hover:border-glow"><p className="font-medium">Homepage</p><p className="mt-1 text-sm text-muted">Hero, About, Right now, The 3D Rule explanations, Journey, Awards, Contact and Footer.</p></Link><div className="glass-card rounded-2xl p-4 text-muted"><p className="font-medium text-foreground">Design and Formula</p><p className="mt-1 text-sm">Their project records, gallery media and shared localized labels are already managed through Projects and Media. Dedicated long-form section editors are the next migration step.</p></div></div></AdminShell>; }

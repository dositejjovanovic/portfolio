import Link from "next/link";
import AdminShell from "@/components/admin/AdminShell";
import { requireAdmin } from "@/lib/admin/auth";
import { listLocalProjects } from "@/lib/content/local";
export default async function AdminProjectsPage() { await requireAdmin(); const projects = await listLocalProjects(); return <AdminShell><div className="flex items-center justify-between gap-4"><h1 className="text-4xl font-semibold tracking-[-.05em]">Projects</h1><Link href="/admin/projects/new" className="rounded-full bg-foreground px-4 py-2 text-sm font-medium text-background">New project</Link></div><div className="mt-6 space-y-2">{projects.map((project) => <Link key={project.slug} href={`/admin/projects/${project.slug}`} className="glass-card block rounded-xl p-4"><p className="font-medium">{project.translations.en.title}</p><p className="mt-1 text-xs text-muted">{project.type} · {project.status} · {project.internalRoute || project.externalUrl || "No destination"}</p></Link>)}</div></AdminShell>; }
